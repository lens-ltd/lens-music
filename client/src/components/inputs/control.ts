/**
 * Shared styles for form controls. Mirrors the source design system's
 * control/label/help trio so every Lens input matches it exactly.
 *
 * controlClass renders through the `field-chrome` utility: 36px height,
 * 6px radius, `#b8c8d3` border, white background, 13px `#173b57` text,
 * `#7d8e9a` placeholder, signal border on focus, danger border on error.
 */
export const controlClass = 'field-chrome';

export const labelClass = 'field-label';

export const helpClass = 'field-help';
