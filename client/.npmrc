# @tanstack/charts declares a peerOptional React 19 entry while this app stays
# on React 18 (verified compatible via tsc + production build). Keep default
# installs working, including the `npm i` step inside `npm run build`.
legacy-peer-deps=true
